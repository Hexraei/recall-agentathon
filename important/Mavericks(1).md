# Repo observations — Mavericks

*An agent read your repository at 19:00 on Saturday. These are observations, not
judgements, and not instructions. Where one disagrees with what you know about your
own code, you are right and it is wrong.*

## In place

- Two back-edges exist, and one routes by reason: a citation failure returns to extraction, any other failure returns to comparison.
- A run was observed drafting three findings, with one rejected by the checker and sent backwards.
- 76 tests pass and the demo runs offline.
- Commits were recorded near all three checkpoints, and PRE-EVENT-ASSETS.md is present.
- With no key the program fails visibly rather than continuing.

## Flagged

- Two branches are unmerged, including the 03:21 commit that wires a different model as the comparison step's judge.
- With no key, one web route returns a server error rather than a handled message.
- A cached report is returned on reload, so that page can be shown without a model call.
- The teacher page shows an unverified banner and a re-run control; the student page shows neither.

---

*Before you demo, run your system once with the network disconnected and compare the output to a connected run. If the two are the same, no model call was involved.*
